//  This will be the main page (Home Page) of the application

import MovieCard from "../components/MovieCard"

const Main = () => {

    return (
        <div>
            <MovieCard />
        </div>
    )
}

export default Main